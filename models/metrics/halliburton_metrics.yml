metrics:
  # Revenue Metrics (from revenue semantic model)
  - name: total_revenue
    description: "Total service revenue across all operations"
    type: simple
    label: "Total Revenue"
    type_params:
      measure:
        name: total_revenue
        join_to_timespine: true
  
  - name: avg_revenue_per_order
    description: "Average revenue per service order"
    type: simple
    label: "Avg Revenue per Order"
    type_params:
      measure:
        name: avg_revenue_per_order
        join_to_timespine: true
  
  - name: order_count
    description: "Total number of service orders"
    type: simple
    label: "Order Count"
    type_params:
      measure:
        name: order_count
        join_to_timespine: true
  
  # Operational Metrics (from operations semantic model)
  - name: active_rig_days
    description: "Total active rig days"
    type: simple
    label: "Active Rig Days"
    type_params:
      measure:
        name: active_rig_days
        join_to_timespine: true
  
  - name: avg_day_rate
    description: "Average daily rig rate"
    type: simple
    label: "Average Day Rate"
    type_params:
      measure:
        name: avg_day_rate
        join_to_timespine: true
  
  - name: total_rig_days
    description: "Total rig operating days"
    type: simple
    label: "Total Rig Days"
    type_params:
      measure:
        name: total_rig_days
        join_to_timespine: true
  
  # Safety Metrics (from safety semantic model)
  - name: total_safety_incidents
    description: "Total safety incidents"
    type: simple
    label: "Total Safety Incidents"
    type_params:
      measure:
        name: total_incidents
        join_to_timespine: true
  
  - name: critical_incidents
    description: "Number of critical safety incidents"
    type: simple
    label: "Critical Incidents"
    type_params:
      measure:
        name: critical_incidents
        join_to_timespine: true
  
  # Equipment Metrics (from equipment semantic model)
  - name: total_equipment_value
    description: "Total current book value of all equipment"
    type: simple
    label: "Total Equipment Value"
    type_params:
      measure:
        name: total_equipment_value
        join_to_timespine: true
  
  - name: avg_equipment_utilization
    description: "Average equipment utilization factor"
    type: simple
    label: "Avg Equipment Utilization"
    type_params:
      measure:
        name: avg_utilization_factor
        join_to_timespine: true
